allow hal_ir_default ir_device:chr_file rw_file_perms;

allow hal_ir_default lirc_device:{
  chr_file
  file
} rw_file_perms;

# Allow IR HAL to r/w to IR device
allow hal_ir_default vendor_ir_spi_device:chr_file rw_file_perms;
